# Server Configuration
PORT=5000
NODE_ENV=development

# MongoDB Configuration
MONGO_URI=mongodb://localhost:27017/cinema-booking

# JWT Secret (Generate a strong random string - minimum 32 characters)
JWT_SECRET=

# Encryption Key (64 character hex string - you can generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
ENCRYPTION_KEY=

# Email Configuration
# Gmail Setup:
# 1. Go to https://myaccount.google.com/security
# 2. Enable 2-Step Verification
# 3. Go to https://myaccount.google.com/apppasswords
# 4. Generate an App Password for "Mail"
# 5. Use the 16-character password below (no spaces)

EMAIL_USER=your_email@gmail.com
EMAIL_PASSWORD=your_16_char_app_password